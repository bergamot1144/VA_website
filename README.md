# Venom Agency - Вебкам Студия

Веб-сайт для вебкам студии с системой авторизации, категориями сайтов и обучением.

## Возможности

- 🔐 Система авторизации (JWT)
- 👥 Система ролей (USER/ADMIN)
- 📁 Категории сайтов
- 🌐 Детальная информация по каждому сайту
- 📚 Обучение в виде текста и видео
- 🎨 Современный темный дизайн с розовыми акцентами
- 🛠️ Админ-панель для управления контентом

## Технологии

### Backend
- Node.js + Express
- TypeScript
- Prisma ORM
- SQLite (можно заменить на PostgreSQL/MySQL)
- JWT для аутентификации
- bcrypt для хеширования паролей

### Frontend
- React + TypeScript
- Vite
- React Router
- Axios
- CSS3 с темной темой

## Быстрый старт

1. Клонируйте репозиторий:
```bash
git clone https://github.com/bergamot1144/VA_website.git
cd VA_website
```

2. Установите зависимости:
```bash
npm run install:all
```

3. Настройте базу данных:
```bash
cd backend
npm run prisma:generate
npm run prisma:migrate
```

4. Создайте файл `backend/.env`:
```env
JWT_SECRET=ваш-очень-сложный-секретный-ключ-минимум-32-символа
PORT=3001
DATABASE_URL="file:./prisma/dev.db"
FRONTEND_URL=http://localhost:5173
```

5. Создайте администратора:
```bash
cd backend
npm run user:create admin ваш_пароль ADMIN
```

6. Запустите проект:
```bash
npm run dev
```

## Подробная инструкция

Смотрите [SETUP.md](./SETUP.md) для детальных инструкций по установке, настройке и развертыванию.

## Структура проекта

```
VA_website/
├── backend/                 # Backend приложение
│   ├── prisma/             # Prisma схема и миграции
│   ├── src/                # Исходный код
│   ├── uploads/            # Загруженные файлы
│   └── scripts/            # Утилиты (управление пользователями)
├── frontend/               # Frontend приложение
│   └── src/                # Исходный код React
└── package.json            # Корневой package.json
```

## Основные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запустить backend и frontend одновременно |
| `npm run build` | Собрать проект для продакшена |
| `cd backend && npm run user:create` | Создать пользователя |
| `cd backend && npm run user:list` | Показать всех пользователей |
| `cd backend && npm run prisma:studio` | Открыть Prisma Studio |

## Развертывание на сервере

См. раздел "Сборка для продакшена" в [SETUP.md](./SETUP.md).

## Лицензия

ISC
