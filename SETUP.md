# Инструкция по установке и запуску Venom Agency

Пошаговая инструкция для установки, настройки и запуска проекта.

## Требования

Перед началом установки убедитесь, что у вас установлены:
- **Node.js** версии 18 или выше ([скачать](https://nodejs.org/))
- **npm** (устанавливается вместе с Node.js)
- **Git** (для клонирования репозитория, если необходимо)

## Шаг 1: Установка зависимостей

### Вариант 1: Установка через корневой package.json (рекомендуется)
```bash
npm run install:all
```

Эта команда автоматически установит зависимости для:
- Корневого проекта
- Backend (Node.js + Express + Prisma)
- Frontend (React + Vite)

### Вариант 2: Установка вручную
```bash
# Установка корневых зависимостей
npm install

# Установка зависимостей backend
cd backend
npm install
cd ..

# Установка зависимостей frontend
cd frontend
npm install
cd ..
```

## Шаг 2: Настройка базы данных

1. Перейдите в папку backend:
```bash
cd backend
```

2. Сгенерируйте Prisma Client (это необходимо сделать перед первым запуском):
```bash
npm run prisma:generate
```

3. Примените миграции базы данных:
```bash
npm run prisma:migrate
```

Это создаст SQLite базу данных в файле `backend/prisma/dev.db` со следующей структурой:
- **User** - пользователи системы (с ролями USER и ADMIN)
- **Category** - категории сайтов
- **Site** - сайты вебкам платформ
- **Lesson** - уроки обучения

## Шаг 3: Настройка переменных окружения

1. Перейдите в папку backend:
```bash
cd backend
```

2. Создайте файл `.env`:
   
   **Для Windows (PowerShell):**
   ```powershell
   Copy-Item .env.example .env
   ```
   
   **Для Windows (CMD):**
   ```cmd
   copy .env.example .env
   ```
   
   **Для Linux/Mac:**
   ```bash
   cp .env.example .env
   ```

3. Откройте файл `backend/.env` и настройте переменные:
```env
JWT_SECRET=ваш-очень-сложный-секретный-ключ-минимум-32-символа
PORT=3001
DATABASE_URL="file:./prisma/dev.db"
FRONTEND_URL=http://localhost:5173
```

**ВАЖНО**: 
- Замените `ваш-очень-сложный-секретный-ключ-минимум-32-символа` на **сложный случайный ключ** (минимум 32 символа)
- Можно сгенерировать ключ онлайн или использовать команду: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
- Никогда не коммитьте файл `.env` в Git!

## Шаг 4: Запуск проекта

### Вариант 1: Запуск backend и frontend одновременно (рекомендуется)

В корневой папке проекта:
```bash
npm run dev
```

Это запустит:
- Backend на `http://localhost:3001`
- Frontend на `http://localhost:5173`

### Вариант 2: Запуск отдельно

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

## Шаг 5: Создание первого пользователя

После запуска backend, создайте первого пользователя. Рекомендуется создать администратора.

### Вариант 1: Через API (если backend запущен)

**Для Windows (PowerShell):**
```powershell
Invoke-RestMethod -Uri "http://localhost:3001/api/auth/register" -Method POST -Headers @{"Content-Type"="application/json"} -Body '{"username":"admin","password":"yourpassword"}'
```

**Для Linux/Mac или с curl:**
```bash
curl -X POST http://localhost:3001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"yourpassword"}'
```

**Примечание:** Первый пользователь, созданный через `/api/auth/register`, будет с ролью `USER` по умолчанию.

### Вариант 2: Через админ-панель (после создания первого пользователя)

1. Войдите в систему с созданным пользователем
2. Если у пользователя роль `ADMIN`, появится ссылка "Панель администратора"
3. В админ-панели → Управление пользователями:
   - Нажмите "+ Добавить пользователя"
   - Заполните логин и пароль
   - Выберите роль "Администратор"
   - Создайте пользователя

### Вариант 3: Назначить роль администратора существующему пользователю

Если у вас уже есть пользователь с ролью `USER`, и вы хотите сделать его администратором:

1. Войдите как существующий пользователь
2. Если у вас есть доступ к админ-панели (только для ADMIN), отредактируйте пользователя
3. Или используйте Prisma Studio (см. ниже)

### Вариант 4: Через Prisma Studio

1. Запустите Prisma Studio:
```bash
cd backend
npm run prisma:studio
```

2. Откройте браузер на `http://localhost:5555`

3. Найдите таблицу `User` и:
   - Создайте нового пользователя или отредактируйте существующего
   - Установите `role = "ADMIN"` для администратора или `role = "USER"` для обычного пользователя
   - **ВАЖНО:** Пароль должен быть захеширован с помощью bcrypt!
   
   Для хеширования пароля используйте скрипт:
   ```bash
   cd backend
   npx tsx scripts/hash-password.ts
   ```
   Введите пароль, скопируйте полученный хеш и вставьте в поле `password` в Prisma Studio.

## Шаг 6: Первый вход в систему

1. Откройте браузер и перейдите на `http://localhost:5173`
2. Введите логин и пароль созданного пользователя
3. После входа вы увидите главную страницу с категориями сайтов

**Роли пользователей:**
- **USER (Пользователь)** - может просматривать категории, сайты и уроки
- **ADMIN (Администратор)** - имеет полный доступ, включая админ-панель для управления контентом

## Добавление контента

### Через админ-панель (рекомендуется)

**Требуется:** Пользователь с ролью `ADMIN`

1. Войдите в систему как администратор
2. Нажмите "Панель администратора" в шапке сайта
3. Используйте разделы:
   - **Пользователи** - управление пользователями и ролями
   - **Категории** - добавление категорий сайтов
   - **Сайты** - добавление сайтов с привязкой к категориям
   - **Уроки** - добавление уроков с текстом и видео

### Через Prisma Studio

1. Запустите Prisma Studio:
```bash
cd backend
npm run prisma:studio
```

2. Откройте браузер на `http://localhost:5555`

3. Добавьте данные в следующем порядке:
   - **Категории (Category)** - сначала создайте категорию
   - **Сайты (Site)** - привяжите к категории через `categoryId`
   - **Уроки (Lesson)** - привяжите к сайту через `siteId`

### Пример структуры данных:

**1. Категория (Category):**
```
name: "Основные сайты"
description: "Популярные вебкам платформы"
order: 0
```

**2. Сайт (Site):**
```
name: "Chaturbate"
url: "https://chaturbate.com"
description: "Одна из самых популярных платформ"
categoryId: [ID категории из шага 1]
```

**3. Урок (Lesson):**
```
title: "Регистрация и настройка профиля"
content: "<p>HTML контент с инструкциями...</p>"
videoUrl: "http://localhost:3001/uploads/videos/video.mp4" или "https://www.youtube.com/embed/VIDEO_ID"
siteId: [ID сайта из шага 2]
order: 0
```

### Добавление видео в уроки

1. Поместите видео файл в папку `backend/uploads/videos/`
   - Поддерживаемые форматы: `.mp4`, `.webm`, `.avi`, `.mov`, `.mkv`, `.m4v`

2. В админ-панели при создании/редактировании урока:
   - Введите URL видео: `http://localhost:3001/uploads/videos/имя_файла.mp4`
   - Или используйте внешний URL (например, YouTube embed): `https://www.youtube.com/embed/VIDEO_ID`

3. Система автоматически определит тип видео и отобразит его правильно

## Решение проблем

### Ошибка: "Cannot find module '@prisma/client'"
**Решение:**
```bash
cd backend
npm run prisma:generate
```

### Ошибка: "Database does not exist" или "Table does not exist"
**Решение:**
```bash
cd backend
npm run prisma:migrate
```

### Ошибка: "JWT_SECRET is not defined"
**Решение:**
1. Убедитесь, что файл `backend/.env` существует
2. Проверьте, что в файле есть строка `JWT_SECRET=ваш-ключ`
3. Перезапустите backend после создания/изменения `.env`

### Ошибка: "EADDRINUSE: address already in use :::3001" (или :::5173)
**Проблема:** Порт уже занят другим процессом.

**Решение для Windows:**
```powershell
# Найти процесс на порту 3001
netstat -ano | findstr :3001

# Убить процесс (замените PID на номер из предыдущей команды)
taskkill /PID <PID> /F

# То же самое для порта 5173
netstat -ano | findstr :5173
taskkill /PID <PID> /F
```

**Решение для Linux/Mac:**
```bash
# Найти и убить процесс на порту 3001
lsof -ti:3001 | xargs kill -9

# Для порта 5173
lsof -ti:5173 | xargs kill -9
```

### Ошибка авторизации / "Неверный логин или пароль"
**Решение:**
1. Проверьте, что пользователь существует в базе данных
2. Убедитесь, что пароль правильно захеширован (если добавляли через Prisma Studio)
3. Попробуйте создать нового пользователя через API или админ-панель

### Ошибка: "Доступ запрещен. Требуется роль администратора"
**Проблема:** Попытка доступа к админ-панели без роли ADMIN.

**Решение:**
1. Убедитесь, что ваш пользователь имеет роль `ADMIN`
2. Проверьте в Prisma Studio или через админ-панель (если у вас есть другой администратор)
3. Перелогиньтесь в системе после изменения роли

### Видео не отображается
**Решение:**
1. Убедитесь, что файл находится в `backend/uploads/videos/`
2. Проверьте, что путь к видео правильный: `http://localhost:3001/uploads/videos/имя_файла.mp4`
3. Убедитесь, что backend запущен и файл доступен
4. Проверьте формат файла (поддерживаются: mp4, webm, avi, mov, mkv, m4v)

### Белый экран в браузере
**Решение:**
1. Откройте консоль браузера (F12) и проверьте ошибки
2. Убедитесь, что frontend запущен на порту 5173
3. Проверьте, что backend запущен и доступен
4. Очистите кеш браузера (Ctrl+Shift+R)

## Сборка для продакшена

### Сборка проекта

```bash
npm run build
```

Это соберет:
- Backend в `backend/dist/` (JavaScript файлы)
- Frontend в `frontend/dist/` (статичные HTML, CSS, JS файлы)

### Запуск в продакшене

**Backend:**
```bash
cd backend
npm start
```

**Frontend:**
Разверните содержимое папки `frontend/dist/` на веб-сервере (например, Nginx, Apache) или используйте хостинг статичных файлов (Netlify, Vercel, и т.д.)

### Важные замечания для продакшена

1. **Безопасность:**
   - Измените `JWT_SECRET` на очень сложный случайный ключ
   - Используйте переменные окружения, не храните секреты в коде
   - Включите HTTPS для безопасности передачи данных

2. **База данных:**
   - Рассмотрите использование PostgreSQL или MySQL вместо SQLite для продакшена
   - Регулярно делайте резервные копии базы данных
   - Настройте автоматические бэкапы

3. **Файлы:**
   - Настройте правильную раздачу статичных файлов (видео, изображения)
   - Рассмотрите использование CDN для медиа-файлов
   - Установите лимиты на размер загружаемых файлов

4. **Мониторинг:**
   - Настройте логирование ошибок
   - Используйте инструменты мониторинга (PM2, и т.д.)
   - Настройте алерты на критические ошибки

---

## Дополнительная информация

### Структура проекта

```
VenomAgencyWebSite/
├── backend/                 # Backend приложение
│   ├── prisma/             # Prisma схема и миграции
│   │   ├── schema.prisma   # Схема базы данных
│   │   ├── dev.db          # SQLite база данных (создается автоматически)
│   │   └── migrations/     # Миграции базы данных
│   ├── src/
│   │   ├── routes/         # API маршруты
│   │   │   ├── admin/      # Админ-маршруты (требуют роль ADMIN)
│   │   │   └── auth.ts     # Маршруты авторизации
│   │   ├── middleware/     # Middleware (авторизация, проверка ролей)
│   │   └── index.ts        # Точка входа сервера
│   ├── uploads/            # Загруженные файлы (видео, изображения)
│   │   └── videos/         # Видео файлы для уроков
│   └── package.json
├── frontend/               # Frontend приложение
│   ├── src/
│   │   ├── pages/          # Страницы приложения
│   │   │   └── admin/      # Страницы админ-панели
│   │   ├── contexts/       # React контексты (AuthContext)
│   │   └── main.tsx        # Точка входа
│   └── package.json
└── package.json            # Корневой package.json с общими скриптами
```

### Основные команды

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запустить backend и frontend одновременно |
| `npm run dev:backend` | Запустить только backend |
| `npm run dev:frontend` | Запустить только frontend |
| `npm run build` | Собрать проект для продакшена |
| `npm run install:all` | Установить все зависимости |
| `cd backend && npm run prisma:generate` | Сгенерировать Prisma Client |
| `cd backend && npm run prisma:migrate` | Применить миграции БД |
| `cd backend && npm run prisma:studio` | Открыть Prisma Studio |

---

**Удачной работы с Venom Agency! 🚀**

